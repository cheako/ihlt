sudo: required
dist: trusty
before_install:
  - sudo apt-get -y install libgnutls28-dev
addons:
  apt:
    packages:
    - libgpgme11-dev
    - libgnutls28-dev
    - libipc-run-perl
    - valgrind

language: c
compiler: gcc

script:
  - make test
